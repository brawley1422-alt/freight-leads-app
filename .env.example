# Copy to .env.local for dev, or export in PM2 env for prod.

# Public base URL — used when building magic-link URLs.
APP_URL=http://localhost:3021

# Who can see /admin.
ADMIN_EMAIL=brawley1422@gmail.com

# SQLite file path. Defaults to <repo>/data/leads.db.
# LEAD_FACTORY_DB=/home/tooyeezy/freight-leads-app/data/leads.db
